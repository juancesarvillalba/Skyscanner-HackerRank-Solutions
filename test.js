// the test framework
let assert = require('assert')

// the input
let stdin = `
`

// the expectation
let expected = `
`
// the test
it('meets the expectation', () =>
    assert.equal(main(stdin), expected))

// the code

function main(input) {
    return '\n';
}
